#include<bits/stdc++.h>
using namespace std;
int main(){
    cout<<"ENter the no you want to enter";

    int n ;
    cin>>n;
    (n >= 33) ? cout<<"Pass" : cout<<"Fail";
    (n>=3) ? cout<<" pass" : cout<<"fail";
}